// This is required to make it possible to build it as a static site,
// if you're interested in having server functionality then you don't necessarily need a +layout.ts at all
export const prerender = true
